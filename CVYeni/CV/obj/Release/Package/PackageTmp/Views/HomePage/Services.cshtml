@model CV.Models.Entity.Tables
@{
    Layout = null;
}

<section id="services" class="services">
    <div class="container">


        <div class="section-title">
            <h2>Service</h2>
            @foreach (var item in Model.ResultServices)
            {
                <p>@item.Description</p>
            }
        </div>
        <div class="row">
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title1</a></h4>
                    <p class="description">@item2.TDesc1</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title2</a></h4>
                    <p class="description">@item2.TDesc2</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title3</a></h4>
                    <p class="description">@item2.TDesc3</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title4</a></h4>
                    <p class="description">@item2.TDesc4</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title5</a></h4>
                    <p class="description">@item2.TDesc5</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title6</a></h4>
                    <p class="description">@item2.TDesc6</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title7</a></h4>
                    <p class="description">@item2.TDesc7</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title8</a></h4>
                    <p class="description">@item2.TDesc8</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title9</a></h4>
                    <p class="description">@item2.TDesc9</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title10</a></h4>
                    <p class="description">@item2.TDesc10</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title11</a></h4>
                    <p class="description">@item2.TDesc11</p>
                </div>
            }
            @foreach (var item2 in Model.ResultServices)
            {
                <div class="col-lg-4 col-md-6 icon-box" data-aos="fade-up">
                    <div class="icon"><i class="bi bi-brightness-high"></i></div>
                    <h4 class="title"><a href="">@item2.Title12</a></h4>
                    <p class="description">@item2.TDesc12</p>
                </div>
            }
        </div>

    </div>
</section>
